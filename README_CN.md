# Vim IDE Enhanced - 强大的 Vim IDE 配置

[English](README.md) | 简体中文

🚀 将 Vim 打造成媲美现代 IDE 的开发环境，专为 C/C++/Python 开发优化。

![Vim Version](https://img.shields.io/badge/Vim-8.0%2B-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)

## ✨ 主要特性

### 🎯 核心功能
- **文件浏览器 (F3)** - NERDTree 提供类似 VSCode 的项目文件树
- **函数列表 (F4)** - Tagbar 显示当前文件的所有函数、结构体、宏定义
- **模糊搜索 (Ctrl-P)** - CtrlP 快速查找项目中的任何文件
- **代码折叠 (空格)** - 一键折叠/展开函数和代码块

### 💻 编程增强
- **智能补全** - 自动补全括号、引号
- **快速注释 (gcc)** - 单行或多行代码注释
- **Cscope 集成** - 强大的代码导航和符号查找
- **语法检查** - ALE 提供实时语法检查和代码格式化

### 🎨 界面美化
- **Airline 状态栏** - 美观的状态栏显示
- **多主题支持** - Gruvbox、OneDark、Molokai 等精美主题
- **行号显示** - 绝对行号 + 相对行号
- **高亮显示** - 当前行/列高亮，80/120列标记线

## 📦 安装步骤

### 方式一：自动安装（推荐）

```bash
git clone https://github.com/YOUR_USERNAME/vim-ide-enhanced.git
cd vim-ide-enhanced
bash install.sh
```

### 方式二：手动安装

```bash
# 1. 备份现有配置
mv ~/.vimrc ~/.vimrc.bak

# 2. 下载配置文件
curl -fLo ~/.vimrc https://raw.githubusercontent.com/YOUR_USERNAME/vim-ide-enhanced/main/vimrc

# 3. 安装 vim-plug 插件管理器
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

# 4. 创建必要目录
mkdir -p ~/.vim/undo ~/.vim/plugged

# 5. 安装插件
vim +PlugInstall +qall
```

## 🔧 系统依赖

### Ubuntu/Debian
```bash
sudo apt-get install -y vim exuberant-ctags cscope curl git
```

### CentOS/RHEL
```bash
sudo yum install -y vim ctags cscope curl git
```

### Arch Linux
```bash
sudo pacman -S vim ctags cscope curl git
```

**注意**：这些工具不是必需的，但安装后可获得完整功能。

## 🎮 快速上手

### 新手三步走

1. **打开 Vim**
   ```bash
   vim your_file.c
   ```

2. **按 F3 打开文件树**
   - 用方向键上下选择文件
   - 按回车打开文件
   - 按 `q` 关闭文件树

3. **按 F4 打开函数列表**
   - 浏览当前文件的所有函数
   - 按回车跳转到函数定义
   - 按 `q` 关闭函数列表

### 5个最常用快捷键

| 快捷键 | 功能 | 记忆方法 |
|--------|------|----------|
| `F3` | 文件树 | F3 = File tree (3个字母) |
| `F4` | 函数列表 | F4 = Functions (4个字母) |
| `Ctrl-p` | 搜索文件 | p = Project search |
| `,w` | 保存 | w = Write |
| `,q` | 退出 | q = Quit |

## 📚 完整文档

- **[快捷键速查表](CHEATSHEET.md)** - 可打印的快捷键参考卡片
- **[详细使用手册](README.md)** - 完整的功能说明和高级技巧

## 🎯 针对不同编程语言的优化

### C/C++ 开发
- ✅ Cscope 符号导航
- ✅ 增强的 C++ 语法高亮
- ✅ 类和成员变量高亮
- ✅ 支持 gcc/clang 语法检查
- ✅ 自动缩进（4空格/Tab）

### Python 开发
- ✅ 语法高亮增强
- ✅ PEP8 风格支持
- ✅ 支持 pylint/flake8 检查
- ✅ 自动缩进（4空格）

### 通用功能
- ✅ Git 集成
- ✅ 自动补全
- ✅ 文件类型检测
- ✅ 智能缩进

## 🛠️ 项目配置

在你的项目根目录执行以下命令，启用完整功能：

```bash
# 生成 ctags（用于函数列表和代码跳转）
ctags -R .

# 生成 cscope 数据库（用于符号查找）
find . -name "*.c" -o -name "*.cpp" -o -name "*.h" -o -name "*.hpp" > cscope.files
cscope -bkq -i cscope.files
```

或者在 Vim 中执行：
```vim
:MakeAll
```

## 🎨 自定义配置

### 更改主题

编辑 `~/.vimrc`，找到以下行并修改：

```vim
silent! colorscheme gruvbox
```

可用主题（9款精选）：

**暖色调：**
- `gruvbox` - 复古护眼配色（默认）⭐ 推荐长时间编程
- `dracula` - 流行暗色主题，紫色调，GitHub 20k+ stars
- `molokai` - 高对比度，适合演示和展示

**中性色调：**
- `onedark` - Atom 风格，现代简洁，广受欢迎
- `palenight` - Material 风格，柔和舒适
- `tokyonight` - 东京夜景主题，近期流行

**冷色调：**
- `nord` - 北欧风格，冷色优雅，适合专注编码
- `iceberg` - 蓝色基调，护眼舒适

**经典：**
- `solarized` - 科学配色的经典主题（支持亮色/暗色切换）

### 自定义快捷键

所有快捷键都在 `~/.vimrc` 中定义，可根据个人习惯修改。

例如，将保存快捷键从 `,w` 改为 `Ctrl-s`：

```vim
" 修改前
nnoremap <leader>w :w<CR>

" 修改后
nnoremap <C-s> :w<CR>
```

### 添加新插件

在 `~/.vimrc` 中找到插件列表，添加你想要的插件：

```vim
call plug#begin('~/.vim/plugged')
" ... 现有插件 ...
Plug 'your/new-plugin'
call plug#end()
```

然后在 Vim 中执行 `:PlugInstall`

## 🔍 常见问题

### Q: 安装完成后，F4 提示 "Tagbar: Exuberant ctags not found!"

**A:** 需要安装 ctags：
```bash
sudo apt-get install exuberant-ctags
```

### Q: 中文注释显示乱码

**A:** 配置已设置 UTF-8，检查你的终端编码：
```bash
echo $LANG  # 应该包含 UTF-8
export LANG=zh_CN.UTF-8
```

### Q: 颜色显示不正常

**A:** 确保终端支持 256 色：
```bash
export TERM=xterm-256color
```

### Q: 插件安装失败

**A:** 检查网络连接，如果无法访问 GitHub，可以配置代理：
```bash
git config --global http.proxy http://your-proxy:port
```

### Q: 如何卸载？

**A:** 删除配置文件和插件目录：
```bash
rm ~/.vimrc
rm -rf ~/.vim/plugged
rm -rf ~/.vim/autoload/plug.vim
mv ~/.vimrc.bak ~/.vimrc  # 恢复原配置
```

## 📖 学习路线

### 第一周：基础操作
- ✅ 掌握 `F3` 文件树
- ✅ 掌握 `Ctrl-p` 文件搜索
- ✅ 掌握 `,w` 保存、`,q` 退出
- ✅ 学会基本的移动：`h j k l`

### 第二周：编辑增强
- ✅ 掌握 `F4` 函数列表
- ✅ 掌握 `空格` 代码折叠
- ✅ 掌握 `gcc` 快速注释
- ✅ 学会分屏：`,vs` `,hs`

### 第三周：窗口管理
- ✅ 掌握 `Ctrl-h/j/k/l` 窗口切换
- ✅ 掌握 `F7/F8` 文件切换
- ✅ 学会多 buffer 操作

### 第四周：代码导航
- ✅ 掌握 `,fg` 查找定义
- ✅ 掌握 `,fs` 查找符号
- ✅ 掌握 `,fc` 查找调用
- ✅ 学会使用 Cscope

## 🤝 参与贡献

欢迎提交问题和改进建议！

1. Fork 本仓库
2. 创建你的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交你的修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启一个 Pull Request

## 🙏 致谢

感谢以下优秀的 Vim 插件项目：

- [vim-plug](https://github.com/junegunn/vim-plug)
- [NERDTree](https://github.com/preservim/nerdtree)
- [Tagbar](https://github.com/preservim/tagbar)
- [vim-airline](https://github.com/vim-airline/vim-airline)
- [CtrlP](https://github.com/ctrlpvim/ctrlp.vim)
- 以及所有其他插件的作者们

## 📝 更新日志

### v1.0.0 (2026-01-06)
- ✨ 初始版本发布
- ✅ 包含 20+ 插件
- ✅ 完整的快捷键配置
- ✅ 支持 C/C++/Python
- ✅ 中英文文档

## 📄 许可证

[MIT License](LICENSE)

---

**⭐ 如果觉得有用，请给个 Star！**

**🐛 发现问题？** [提交 Issue](https://github.com/YOUR_USERNAME/vim-ide-enhanced/issues)

**💬 有问题？** 欢迎在 Issues 中讨论交流！

